---
description: Run Glitch UTM, campaign, conversion, install-journey, social, influencer, and ad attribution reports.
argument-hint: [report_keys] [filters]
---

Use Glitch MCP tool `glitch_get_attribution_reports` for the selected title.

Arguments I provided: $ARGUMENTS

Keep observed attribution, correlation, and missing tracking coverage distinct. Use scheduler_id only when an advertised ad report requires it.
