---
description: Check Glitch title subscription, trial, plan, and credits.
argument-hint: [title_id]
---

Use Glitch MCP tool `glitch_get_billing_status` to check billing, subscription, trial, plan, and credit state for a title.

Arguments I provided: $ARGUMENTS

If I provided a title id, pass it as `title_id`. Otherwise use the selected title, or call `glitch_list_titles` and ask me which title to use.
Summarize whether paid agent runs are available and include any billing dashboard link returned by Glitch.

