---
description: Discover Glitch social platforms, connections, permissions, and operations.
argument-hint: [title_id]
---

Use Glitch MCP tool `glitch_get_social_capabilities` for the selected title.

Arguments I provided: $ARGUMENTS

Summarize connected schedulers, effective platform capabilities, known limitations, and the exact operation names relevant to what I am trying to do. Never request or display OAuth credentials.
