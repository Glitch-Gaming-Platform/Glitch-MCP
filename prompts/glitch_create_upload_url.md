---
description: Create a short-lived Glitch upload URL.
argument-hint: file_name mime_type size_bytes [title_id] [run_id]
---

Use Glitch MCP tool `glitch_create_upload_url` to create a short-lived upload URL for attaching a file to a Glitch Agent title or run.

Arguments I provided: $ARGUMENTS

Use provided file name, MIME type, size in bytes, title id, and run id when clear.
If any required value is missing, ask me for it.
Return the upload instructions and relevant attachment context without exposing sensitive values unnecessarily.

