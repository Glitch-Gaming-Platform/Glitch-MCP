---
description: List artifacts for a Glitch Agent run.
argument-hint: run_id [title_id]
---

Use Glitch MCP tool `glitch_list_artifacts` to list downloadable files and hosted report artifacts for a Glitch Agent run.

Arguments I provided: $ARGUMENTS

Treat the first run-like id as `run_id`. Use a provided title id if present.
If `run_id` is missing, ask me for it.
Summarize artifacts by type, purpose, and link.

