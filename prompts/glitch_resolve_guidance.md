---
description: Resolve Glitch guidance with interactive prompts when available.
argument-hint: [title_id] [run_id] [guidance_id] [limit]
---

Use Glitch MCP tool `glitch_resolve_guidance` to present open stop-gate questions and route answers back to Glitch.

Arguments I provided: $ARGUMENTS

Use provided title id, run id, guidance id, and limit when clear. If no title is selected, call `glitch_list_titles` and ask me which title to use.
If interactive elicitation is unavailable, show a readable question list and ask me for answers.

