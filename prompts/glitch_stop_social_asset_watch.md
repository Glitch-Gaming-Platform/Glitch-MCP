---
description: Stop the local Glitch social asset watcher.
argument-hint: project_root
---

Use Glitch MCP tool `glitch_stop_social_asset_watch` to disable the local Glitch social asset folder watcher for a project.

Arguments I provided: $ARGUMENTS

Treat the first path-like argument as `project_root`. If it is missing, ask me for it.
Summarize whether the watcher was stopped.

