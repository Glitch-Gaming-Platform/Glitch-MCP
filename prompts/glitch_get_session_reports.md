---
description: Run Glitch session, retention, active-user, cohort, and funnel reports.
argument-hint: [report_keys] [filters]
---

Use Glitch MCP tool `glitch_get_session_reports` for the selected title.

Arguments I provided: $ARGUMENTS

Narrow with report_keys when the request is focused. Summarize successful metrics and unavailable reports separately.
