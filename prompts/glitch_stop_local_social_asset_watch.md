---
description: Stop the Glitch local social asset watcher.
argument-hint: [project_root]
---

Use Glitch MCP to disable the local social asset watcher.

Optional arguments I provided: $ARGUMENTS

Use the current project root if available; otherwise ask me for `project_root`.
Call `glitch_stop_social_asset_watch`.

