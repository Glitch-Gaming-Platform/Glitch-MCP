---
description: Reject a proposed or approval-needed Glitch action.
argument-hint: action_id [title_id] [reason]
---

Use Glitch MCP tool `glitch_reject_action` to reject a proposed or approval-needed Glitch Agent action.

Arguments I provided: $ARGUMENTS

Treat the first action-like id as `action_id`. Use a provided title id and reason if present.
If the action id is missing, ask me for it.
After rejecting, summarize the rejected action and reason.

