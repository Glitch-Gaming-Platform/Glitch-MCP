---
description: Upload a local file to a Glitch title or run.
argument-hint: file_path [title_id] [run_id]
---

Use Glitch MCP tool `glitch_upload_file` to upload a local image, video, or document to a Glitch title or run.

Arguments I provided: $ARGUMENTS

Treat the first path-like argument as `file_path` unless I provide base64 content instead. Use provided title id, run id, file name, and MIME type when clear.
Before uploading, confirm that I intentionally asked to send this local file to Glitch. If the file path or destination is unclear, ask me first.
After upload, summarize the uploaded file, attachment id, and how it can be used by a run.

