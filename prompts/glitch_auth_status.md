---
description: Check Glitch MCP authentication and title access.
argument-hint: [title_id]
---

Use Glitch MCP tool `glitch_auth_status` to check whether the current token can access Glitch MCP.

Arguments I provided: $ARGUMENTS

If I provided a title id, pass it as `title_id`. Otherwise call the tool without `title_id`.
Summarize whether authentication works, whether a title is selected, and any next setup step needed.

