---
description: Scan local captures and marketing folders for social content.
argument-hint: [title_id] [project_root] [since_hours]
---

Use Glitch MCP to scan local game assets for social content candidates.

Optional arguments I provided: $ARGUMENTS

If no title is selected, call `glitch_list_titles` and ask me which title to use.
Use the current project root if available; otherwise ask me for `project_root`.
Show candidate ids, scores, suggested platforms, and reasons.
Do not upload anything yet.

