---
description: List timeline events for a Glitch Agent run.
argument-hint: run_id [title_id] [after_id] [limit]
---

Use Glitch MCP tool `glitch_list_run_events` to list user-visible timeline events for a Glitch Agent run.

Arguments I provided: $ARGUMENTS

Treat the first run-like id as `run_id`. Use a provided title id, `after_id`, and `limit` if present.
If `run_id` is missing, ask me for it.
Summarize the event timeline newest-to-oldest or in natural chronological order, whichever is clearer.

