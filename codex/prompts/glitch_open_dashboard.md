---
description: Return Glitch dashboard links for title, run, action, or billing.
argument-hint: [kind] [title_id] [run_id] [action_id]
---

Use Glitch MCP tool `glitch_open_dashboard` to return Glitch dashboard links for a title, run, action, or billing screen.

Arguments I provided: $ARGUMENTS

Infer `kind` from my request when clear: title, run, action, or billing.
Use provided title id, run id, and action id when present. If required context is missing, ask me for it.
Return the dashboard links with a short label for each.

