---
description: List Glitch game titles available to this MCP token.
argument-hint: [include_archived]
---

Use Glitch MCP tool `glitch_list_titles` to list available game titles.

Arguments I provided: $ARGUMENTS

If I ask for archived titles, set `include_archived=true`; otherwise use the default.
Show each title with its id and a short selection note.

