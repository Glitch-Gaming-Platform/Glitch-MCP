---
description: List pending, proposed, or executed Glitch actions.
argument-hint: [title_id] [run_id] [status] [limit]
---

Use Glitch MCP tool `glitch_list_pending_actions` to list proposed, guidance-needed, approval-needed, approved, or executed actions for a title.

Arguments I provided: $ARGUMENTS

Use provided title id, run id, status, and limit when clear. If no title is selected, call `glitch_list_titles` and ask me which title to use.
Summarize each item by status, risk, likely impact, required decision, and dashboard link.
Do not approve or execute anything from this command.

