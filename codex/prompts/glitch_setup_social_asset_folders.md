---
description: Create local Glitch social asset folders.
argument-hint: project_root [folders]
---

Use Glitch MCP tool `glitch_setup_social_asset_folders` to create local Glitch social asset folders and config under a game project.

Arguments I provided: $ARGUMENTS

Treat the first path-like argument as `project_root`. Use custom folders only if I clearly provide them.
Set `confirm=true` only when I explicitly ask you to create/setup the folders. If the project root is missing, ask me for it.
After setup, summarize created folders and config path.

