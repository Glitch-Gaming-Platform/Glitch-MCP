---
description: Start the local Glitch social asset watcher.
argument-hint: project_root [interval_hours]
---

Use Glitch MCP tool `glitch_start_social_asset_watch` to activate the opt-in local scan timer for Glitch social asset folders.

Arguments I provided: $ARGUMENTS

Treat the first path-like argument as `project_root`. Use interval, folders, and scan settings only if I clearly provide them.
Set `confirm=true` only when I explicitly ask you to start the watcher. If the project root is missing, ask me for it.
Summarize watcher status and whether an immediate scan ran.

