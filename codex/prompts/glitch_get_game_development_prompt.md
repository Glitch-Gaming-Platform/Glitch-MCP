---
description: Retrieve a complete Glitch AI game-development prompt.
argument-hint: [prompt_id]
---

Use Glitch MCP tool `glitch_get_game_development_prompt`.

Arguments I provided: $ARGUMENTS

Treat the first slug-like value as `prompt_id`. Return or apply the complete Markdown, including its required game-documentation section. These are public editorial prompts, not private Glitch Agent planner prompts.
