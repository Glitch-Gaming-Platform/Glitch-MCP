---
description: Run a deterministic title-scoped Glitch social operation.
argument-hint: <operation> [arguments]
---

Use Glitch MCP tool `glitch_social_operation` to perform the social operation I requested.

Arguments I provided: $ARGUMENTS

If the exact operation name or required arguments are unclear, call `glitch_get_social_capabilities` first. Set `confirm=true` only when I explicitly asked for the mutation, publish, engagement, message, sync, disconnect, or destructive action. Never pass OAuth tokens, passwords, API keys, or credentials.
