---
description: List open or answered Glitch guidance requests.
argument-hint: [title_id] [run_id] [status] [limit]
---

Use Glitch MCP tool `glitch_list_guidance` to list open or answered guidance requests for a title or run.

Arguments I provided: $ARGUMENTS

Use provided title id, run id, status, and limit when clear. If no title is selected, call `glitch_list_titles` and ask me which title to use.
Summarize each guidance item, what decision is needed, and any dashboard link.

