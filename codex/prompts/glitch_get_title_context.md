---
description: Fetch safe Glitch title context.
argument-hint: [title_id]
---

Use Glitch MCP tool `glitch_get_title_context` to fetch safe, subscription-gated context for a game title.

Arguments I provided: $ARGUMENTS

If I provided a title id, pass it as `title_id`. Otherwise use the selected title, or call `glitch_list_titles` and ask me which title to use.
Summarize the title context in a practical way for planning agent work.

