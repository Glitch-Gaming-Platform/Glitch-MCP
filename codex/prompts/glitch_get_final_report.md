---
description: Fetch the final or partial report for a Glitch Agent run.
argument-hint: run_id [title_id]
---

Use Glitch MCP tool `glitch_get_final_report` to fetch the human-friendly final or partial report for a Glitch Agent run.

Arguments I provided: $ARGUMENTS

Treat the first run-like id as `run_id`. Use a provided title id if present.
If `run_id` is missing, ask me for it.
Summarize the report clearly and include dashboard/artifact links returned by Glitch.

