---
description: Choose and apply the right Glitch AI game-development prompt.
argument-hint: [prompt_id] [project context]
---

Use Glitch's public AI Game Development Prompt library for this task.

Optional prompt id and project context: $ARGUMENTS

If a prompt id is not clear, call `glitch_list_game_development_prompts`, recommend the closest situation, and retrieve it with `glitch_get_game_development_prompt`. Use the returned Markdown as the task instructions, replace placeholders with known project facts, and complete the work instead of merely repeating the prompt.

Every prompt requires creating or updating the appropriate game documentation and listing those files in the final report.
