---
description: Fetch a Glitch Agent run by id.
argument-hint: run_id [title_id]
---

Use Glitch MCP tool `glitch_get_agent_run` to fetch a durable Glitch Agent run.

Arguments I provided: $ARGUMENTS

Treat the first run-like id as `run_id`. Use a provided title id if present; otherwise use the selected title.
If `run_id` is missing, ask me for it.
Summarize status, report availability, open guidance, pending actions, and dashboard links.

