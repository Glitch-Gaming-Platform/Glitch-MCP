---
description: Run one canonical read-only Glitch analytics report.
argument-hint: <report_key> [filters]
---

Use Glitch MCP tool `glitch_get_analytics_report` for the selected title.

Arguments I provided: $ARGUMENTS

Call `glitch_get_analytics_capabilities` first if the report key or filters are unclear. Preserve empty and unavailable states instead of treating missing data as zero.
